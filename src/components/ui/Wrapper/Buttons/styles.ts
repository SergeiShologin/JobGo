import { styled } from 'styled-components'
import { IButtonProps } from './types'

export const ButtonsWrapper = styled.div<IButtonProps>`
  display: flex;
  gap: 8px;
  margin-top: 16px;
  margin-left: 40px;
  margin-right: 40px;
`